#include <stdio.h>
#include "math.h"

int main() {
    float x_final = 12.0;
    float y;
    float a;
    printf("Введите a: ");
    scanf("%f", &a);
    printf("a value:", a);
    // for (float x_initial = -13.0; x_initial < x_final; x_initial += 2.5) {
    //     //проверка что знаменатель не равен нулю
    //     //bruh
    // }
    return 0;
} 